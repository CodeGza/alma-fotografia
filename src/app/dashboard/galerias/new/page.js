import CreateGalleryForm from '@/components/dashboard/galeria/CreateGalleryForm';

/**
 * New Gallery Page
 * 
 * Ruta: /dashboard/galerias/new
 * 
 * Página para crear nueva galería.
 * Solo renderiza el formulario CreateGalleryForm.
 */
export default function NewGalleryPage() {
  return <CreateGalleryForm />;
}